import sys
r = sys.stdin.readline


M, N = map(int,r().split())
sc, stack = [],[]
for i in range(N):
    row = list(map(int, r().split()))
    screen.append(row)

def alg(i,j)
    if sc[i][j]=="c" or sc[i][j]==".":
        alg(i+1)
    else:
        if sc[i][j+1]
            


print(bfs_tomato(M,N,box))